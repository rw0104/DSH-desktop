# DSH Desktop v2.0.10

> Windows 安装包延续透明 unsigned 发布策略：安装器和主程序均验证为 `NotSigned`，GitHub Release 同时记录资产大小与 SHA-256。Windows 可能显示 SmartScreen 或 Unknown Publisher；SHA-256 完整性校验不等同于 Authenticode 发布者身份。

## 本轮升级

- 产品工作台从 `dsh-better-sidebar@0.15.1` 升级到 `0.16.1`，官方 DeepSeek Harness 继续固定在已验证的 `0.1.1-rc.2`，未修改上游子模块源码。
- 侧边栏支持把 Tab 拖入主会话区形成自由窗口，并保持 `registerTab`、`openTab`、`closeTab` 和插件 Tab 生命周期兼容。
- 新增默认关闭的 `sidebar_open` 模型工具，可在用户明确启用后打开文件、目录和 HTTP(S) 页面；关闭设置不会注入该工具。
- Markdown 预览支持经 DOMPurify 消毒的内嵌 HTML、目录大纲和本地相对图片；编辑器增加 Vue 与更多语言高亮。
- Git 面板支持多仓库与 linked worktree，`0.16.1` 对状态数量、仓库发现、超时和恢复入口设置上限，避免大工作区冻结与重启循环。
- 上游修复文件 API 工作区越界和符号链接检查，并修复 npm DSH `0.1.1-rc.x` 下内部 `ctx.betterSidebar` 读取导致的页面崩溃。
- CI 发现并修复 Desktop Review 在 POSIX 上对反斜杠路径的 containment 漏洞：校验与最终 `resolve()` 现在使用同一归一化值，`..\\outside.ts` 会稳定拒绝；Deliverable 复制测试按服务既有 `realpath` 合约比较 macOS `/private/var` 规范路径。

## 兼容性与依赖决策

- `0.15.1` 与 `0.16.1` 发布包的 `cordis.patch.yml` SHA-256 均为 `D363ACAD7F1521559194A3E04DE06FE68974B4A7B1AD3CC74DB54BCA63C4248B`；Loader 插入行未变化。
- `BetterSidebarService` 的 Desktop 消费面保持兼容，只新增 `floatWindows` feature。Desktop 继续通过 `ctx.get('betterSidebar')` 和最小结构接口注册 `desktop:changes` Tab。
- 上游移除了公开 `cordis` peer，改用 `@deepseek-ai/cordis` 类型基底；Desktop 自有的 `cordis@4.0.0-rc.8` 运行时接线不变。
- `@huanlin/dsh-plugin-better-locale` 是可选 peer，本产品未捆绑；中文和英文路径不依赖它。
- 该版本在 npm 发布未满 Yarn 默认 24 小时隔离期时完成审计，因此 `.yarnrc.yml` 只预批准精确描述符 `dsh-better-sidebar@0.16.1`，未放宽其他包。

## 验证记录

- `corepack yarn install --immutable`：通过；sidebar 相关 peer 均满足或为可选 peer。
- 侧边栏/Profile/Workbench 聚焦回归：6 个测试文件，103 passed。
- 完整 `corepack yarn check`：Community Market 268 passed；Desktop 703 passed、11 skipped；201 个 first-party runtime 节点闭包、Loader/Profile/CLI 和 691 个生产依赖许可证检查通过。
- Windows package preflight：14 个测试文件，203 passed；runtime closure 通过。
- Electron Builder afterPack、ASAR 完整性、诊断 Worker smoke 和 Windows PE installer verifier：通过；独立 afterPack 重放再次通过。
- 成品内 manifest：`dsh-plugin-desktop@2.0.10`、`dsh-better-sidebar@0.16.1`；包含 `floatWindows`、`sidebar_open` 和 `statusTruncated` 标记。
- 路径修复后完整 `corepack yarn check` 与 Windows `dist:win` 均重新执行；成品 bundle 明确包含 `resolve(cwd, value)`，未复用第一次候选安装器。
- Profile smoke 按真实宿主验证盘符参数：Windows runner 要求非空大写盘符，macOS/Linux runner 要求参数缺失；这与产品“仅在枚举到盘符时写 URL 参数”的契约一致。
- 许可证 verifier 对标准 `license` / `license.md` / `license.txt` 文件名做跨平台大小写归一化；Sidebar 传递依赖 `khroma@2.1.0` 随包提供小写 `license`，内容和上游 SPDX 均为 MIT。
- GitHub CI [32915919433](https://github.com/rw0104/DSH-desktop/actions/runs/32915919433)：Ubuntu 完整 check、Windows check/installer/portable、macOS check/packaged smoke 和 upstream command 全部通过。
- CI 全绿后重新生成第三方清单，补入 Sidebar 新增的 `@codemirror/lang-vue@0.1.3` 与 `react-icons@5.7.0`；NSIS effective config 明确使用该清单，最终安装器从清单提交重新本地构建。

## 安装包校验

- 文件：`DSH-Desktop-2.0.10-x64-Setup.exe`
- 大小：`276,771,959` bytes
- SHA-256：`B37BA8A0F41BD3EE1DE2D7BCC34BFD93D51F2988494DCFED1EDAA838F253038D`
- `latest.yml` 大小：`342` bytes
- `latest.yml` SHA-256：`85D7D5B3C2F0E2153DEC96AAB365E958E61660BC41348DBCACD1766AB20EEB25`
- unpacked 主程序 SHA-256：`A203E1FE66886FD341C1570B2F52F1A9F6AB74BE62075C1F9C67A8D73A52C6C5`
- FileVersion / ProductVersion：`2.0.10` / `2.0.10.0`
- Authenticode：`NotSigned`
- 本地构建源码：`baad5023bec255518f97a98a7dd13b53ea69b5cb`

## 已知限制

- 本版未配置受信 Windows 代码签名证书；安装器真实性仍需结合本仓库 Release、固定文件名、大小和 SHA-256 验证。
- 本轮只构建和发布 Windows x64 安装器；未生成新的签名、公证 macOS DMG。
- Better Sidebar 新增资源使安装器较 v2.0.9 增加 `22,292,410` bytes（约 `8.76%`）。

## 关联文档

- [上游同步台账](../upstream-sync.md)
- [Better Sidebar v0.16.0](https://github.com/omdsh-dev/DSH-better-sidebar/releases/tag/v0.16.0)
- [Better Sidebar v0.16.1](https://github.com/omdsh-dev/DSH-better-sidebar/releases/tag/v0.16.1)

## GitHub 发布结果

- Release：[DSH Desktop v2.0.10](https://github.com/rw0104/DSH-desktop/releases/tag/v2.0.10)，发布时间 `2026-08-26T10:52:33Z`。
- GitHub `releases/latest` 返回 `v2.0.10`；Release 为非 draft、非 prerelease。
- Annotated tag `v2.0.10` 解引用到 `14f50e727d7d9f395b52d3e7546898c354c22ac3`，包含最终 notice-build 和发布日志。
- GitHub Setup 资产大小 `276,771,959` bytes，digest `sha256:b37ba8a0f41bd3ee1de2d7bcc34bfd93d51f2988494dcfed1edaa838f253038d`，与本地一致。
- GitHub `latest.yml` 资产大小 `342` bytes，digest `sha256:85d7d5b3c2f0e2153dec96aab365e958e61660bc41348dbcacd1766ab20eeb25`，与本地一致。
- 最终 CI [32935865273](https://github.com/rw0104/DSH-desktop/actions/runs/32935865273) 全绿；Release 上传的是本地构建资产，不是 CI 构建产物。
