# DSH Desktop v2.0.8

## 修复内容

- 官方 DeepSeek Harness 子模块升级到 `dsh-v0.1.1-rc.2`，所有桌面/Market DSH package family 同步到 `0.1.1-rc.2`。
- Better Sidebar 升级到 npm `0.15.1`，保留 Desktop 自有 Host/Client 适配边界。
- 恢复 Windows “选择工作区目录”弹窗中的实际盘符选择器；当前机器检测到的 `C:`、`D:`、`E:` 会显示为 `C盘 (C:)`、`D盘 (D:)`、`E盘 (E:)`。
- 修复盘符选择异步清空导致的 `:\` 路径问题；选择 C 后提交路径为 `C:\`。
- 产物复制路径/内容和视觉模型能力误判按计划留到本安装包之后单独开发，不混入本版本发布变量。

## 已确认的后续问题

发布后确认 Windows 成品会过滤 Vision Toolkit 运行时清单要求的 `CHANGELOG.md`，导致该插件在每次启动时报告运行环境未就绪；这与用户配置的 API Key 无关。后续修复结果和正式产品决策记录在公开的 [上游同步台账](../upstream-sync.md) 与 v2.0.9 发布说明中；调查过程文档保留在本地开发资料中，不进入公开仓库。

## 上游迁移记录

- 旧 Harness pin：`dsh-v0.1.0-rc.8` / `141eb6fef83422698aef7a981029e843e8161534`。
- 新 Harness pin：`dsh-v0.1.1-rc.2` / `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`。
- 同步台账：[`upstream-sync.md`](../upstream-sync.md)。

本机 NSIS 3.0.4.1 不支持参考 patch 中的 `ManifestLongPathAware` 指令。本版本保留 app-builder-lib 的 macOS 签名修复，移除这两条不兼容的 NSIS 指令；该差异已写入 patch 基线并通过安装器 verifier。

## 验证

- `corepack yarn install --immutable`：通过。
- `corepack yarn typecheck`：通过。
- `corepack yarn check`：Market 268/268；Desktop 677 passed、11 skipped；runtime closure、Loader/Profile 和 licenses 通过。
- Packaged Electron smoke：Renderer URL 带 `dsh-desktop-drives=CDE`；盘符选项为 C/D/E；选择 C 后输入框为 `C:\`，错误为空。证据：[directory-picker-2.0.8.png](../evidence/electron/directory-picker-2.0.8.png)。
- Windows x64 NSIS installer verifier：通过。

## 安装包校验

- 文件：`dsh-plugin-desktop/dist/DSH-Desktop-2.0.8-x64-Setup.exe`
- 大小：263,143,129 bytes
- SHA-256：`73ACCE83E921760291BE10B8C49897CBB203698EB83A27EFBBF6CAFEC31318D2`
- GitHub Release：[DSH Desktop v2.0.8](https://github.com/rw0104/DSH-desktop/releases/tag/v2.0.8)
