# DSH Desktop v2.1.0-rc.2

本候选版本完成官方 DeepSeek Harness `dsh-v0.1.2-rc.1` 的完整接入。Desktop Host、Web Client、Profile/Loader、Community Market、Voice/Agent 合同和 Windows 打包闭包均已迁移；Better Sidebar 使用正式 `0.18.0`（peer 明确支持 RC1），不混入 main/alpha 快照。

## 主要变更

- DSH package family 与 Cordis 基础设施统一到 Harness `0.1.2-rc.1` / Cordis `4.0.2`，补齐 RC1 新增的一方 peer 闭包。
- 用 Session Controller 分页替换旧 APIProxy history 读取；deliverable verifier 支持 RC1 event/chunk history，并保留路径授权和文本/二进制安全边界。
- 重新生成 RC1 app boot、directory picker、workspace、attachment/conversation、settings/model capability、deliverables、DeepSeek streaming、Win32 process 和 Sidebar 兼容补丁。
- 保留 Compatibility/Advanced Profile 行为、Community Market receipt/restart 边界、Vision `inputModalities` admission 和 Windows 隐藏控制台策略。

## 验证

- `$env:YARN_NPM_MINIMAL_AGE_GATE='0'; corepack yarn install --immutable --mode=skip-build` (RC1 prerelease age gate is intentionally disabled for this candidate)
- `corepack yarn typecheck`
- `corepack yarn test`: Desktop 90 个测试文件、780 项通过（11 项按条件跳过）；Market 19 个文件、275 项通过。
- `corepack yarn check`: runtime closure 228 个一方节点闭合，生产许可 744 个包，Loader/Profile smoke 通过。
- Windows x64 installer smoke: 419 个物理文件、675,533,931 bytes、18,012 个 ASAR 条目，`verify-packaged-profile`、Installer verifier 和 portable verifier 均通过。
- Reviewed catalog live replay: 1024Store provider/normalized `500/500`，dshfind `13073/13072`；两者均通过生产 restricted client 的 gzip、大小和 item headroom 限制。完整 endpoint、耗时和字节证据见 [upstream sync ledger](../upstream-sync.md)。

## 本地 Windows 产物

```text
DSH-Desktop-2.1.0-x64-Setup.exe
size: 222,841,605 bytes
sha256: A4E2A4ABF73A6FFC12DD8F25CB7F80C3734E8645CC59305301B60A5D9293CBC3
signature: NotSigned (test artifact)
portable: DSH-Desktop-2.1.0-x64-Portable.zip, 222,326,218 bytes, sha256 FAEDD20202D05BDD74B17839186AD704EC8177630F7FAAD9630546E78342EC0F
latest.yml sha256: 88AF0EE803EF3964D3CB7818B3E6DE16AE73000B140ADAE8BFAF8C603853FE70
```

这是 prerelease 候选，不代表稳定发布。真实账号语音、多轮 Agent、审批/打断/重连、真实用户 Profile/Session/receipt 迁移回滚和 macOS 设备 smoke 仍需在发布前补做；代码签名、远端资产回读和正式 Release tag 也保持独立准入。
