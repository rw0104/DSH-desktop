# DSH Desktop 2.2.6 本地测试候选

2.2.6 修复图片模型经普通聊天接口调用失败的问题，并将图片协议、结果处理和异步任务执行拆分为可复用的适配机制。普通会话继续直接使用选中的供应商和模型。

本次分支同步包含 2.2.4—2.2.6 的累计修复，最终行为如下：

- 撤销 2.2.4 引入的“受控生成”设置、模式命令和工具锁；普通会话直接使用所选模型的原生图片能力。
- 模型发现保留图片输入与图片输出的区别；OpenRouter 请求包含全部输出类型的目录，兼容供应商发现模型时复用该供应商已保存的凭据。
- 原生图片与普通兼容聊天接口实际返回的图片均可保存到附件存储，供展示及后续引用；地区限制错误与密钥错误分别呈现。

- 阿里云已知 Qwen-Image 2.0 模型自动使用同一工作区的原生生成接口，支持临时图片链接保存和后续编辑引用。
- 修复自定义供应商无正文 400/413 被误判成上下文超限、进而触发压缩的问题。
- 供应商可以复用已有图片协议，新协议通过受信任 Host 插件注册；共用执行器负责结果转换、下载、持久化、取消及异步任务查询。
- 供应商级图片协议默认值只影响图片输出模型，文本模型保留原聊天接口。未知协议会给出明确错误，不自动尝试其他供应商。
- 图片调用退出聊天层的整次自动重试，防止任务查询失败后重复提交生成；普通聊天重试保持原行为。
- 语音浮条支持拖动、边界约束和默认避让，任务审批状态不再被运行状态覆盖；改进输入/输出音频反馈、播放时序采样及 2D 降级动画。
- 修复高级布局 details 插槽缺少会话作用域时可能出现的空白界面。

接入方式与验收要求见[图片协议接入说明](../plugin-image-protocols.md)。本轮使用本地接口测试和实际包内界面验证；真实账号的模型权限、地区和生成结果需要另行实测。

新增供应商优先复用已有协议及能力配置；真正不同的协议通过受信任 Host 插件适配，无需修改 Agent 主流程。协议和能力未声明时仍需接入维护者核对供应商文档，不能凭模型名字保证可用。

Harness 保持官方 RC1 发布包及原有 submodule pin，Sidebar 保持 0.18.0，pi-ai 保持 0.84.4。官方开发分支及桌面参考仓库有更新，本轮未将未经兼容性审查的提交混入修复，下一步迁移范围记录在[同步记录](../upstream-sync.md)。这是本地测试候选，不代表已经发布 GitHub Release。

验证：Desktop 857 passed / 11 skipped，Market 275 passed，45 项图片/能力专项和实际包内两轮图片流程通过。安装器 224,117,189 bytes，SHA-256 `1C0338C0709913E9806877061B788E203277261728CF4036045B1B46648CC95E`，未签名；编译源码 `a7e02445ee32`。
