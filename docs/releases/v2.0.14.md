# DSH Desktop v2.0.14

DSH Desktop v2.0.14 是 `v2.0.13` 的立即修正版：已有 Profile 私有 `mode`、`port`、`logLevel` 在启动时不仅驱动 Host 组合，也同步回 settings service，设置页面显示值与当前运行配置保持一致。

## 修复内容

- 首次使用 Profile 时，从共享旧 `settings.yaml` 导入三个 Desktop 字段并写入 Profile 私有状态。
- Profile 已有私有状态时，以私有状态为准启动，并在 Host boot 后同步 settings service；不会把另一个 Profile 的共享旧值显示为当前值。
- 同步只覆盖 `mode`、`port`、`logLevel`，不会复制窗口、更新、日志目录、安装 ID 或其他插件 namespace。
- 保留 `v2.0.13` 的 Windows installer quit handoff、精确进程路径范围、30 秒优雅退出预算和 ZIP/NSIS 恢复语义。

## 兼容与边界

- DeepSeek Harness 继续固定 `0.1.1-rc.2` package family，Better Sidebar 继续固定 `0.17.1`。
- 不接入 Harness alpha、Remote gateway、一次性 token、参考桌面私有服务或 7z 原地解压。
- Windows Setup 与主程序仍未配置 Authenticode 签名；SHA-256 只用于完整性核对。

## 验证结果

| Gate | Result |
| --- | --- |
| Profile reconciliation focused tests | Passed; focused set 77 tests |
| Desktop typecheck | Passed |
| `corepack yarn check` | Market 270 passed; Desktop 734 passed, 11 skipped; closure 201; licenses 691 |
| Windows package preflight | Passed; 210 tests, runtime closure 201 |
| Installer verifier | Passed |
| Running-app quit probe | Passed; exit code 0, marker cleanup verified |

## Windows 安装包

| File | Size | SHA-256 |
| --- | ---: | --- |
| `DSH-Desktop-2.0.14-x64-Setup.exe` | 330,862,733 bytes | `423B00F77911C8EAFDC3EF711B2DE0804FD08C2FC8D23D82058F8927C5E57E53` |
| `latest.yml` | 342 bytes | `346513C34E023ADF6EFC7D36472ECF0ED5328B3F0D300522F17AAB3C3228F41D` |

unpacked 主程序为 225,552,896 bytes，SHA-256 `AF08F57C3212BF48BD402CDD1647D469564E6640476FA27E626ED9E6B8DAA2DA`，FileVersion `2.0.14`，ProductVersion `2.0.14.0`，Authenticode `NotSigned`。Setup 同样为 `NotSigned`；`latest.yml` 的 SHA-512 与 Setup 一致。

Release 只上传 Setup 与 `latest.yml`。

## 发布结果

- GitHub Release：[DSH Desktop v2.0.14](https://github.com/rw0104/DSH-desktop/releases/tag/v2.0.14)，发布时间 `2026-08-29T16:26:12Z`，为 Latest、非 draft、非 prerelease。
- Annotated tag `v2.0.14` 解引用到本地安装包证据提交 `f76dab10b72749b52d699891540ebb080cee5bf5`；tag object 为 `d183ec85213b4446b6c3068277c10e469cab473d`。
- GitHub Setup 与 `latest.yml` 的远端 size/digest 和上述本地结果一致；Release 只包含这两个版本化资产。
