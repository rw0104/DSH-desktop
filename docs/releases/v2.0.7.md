# DSH Desktop v2.0.7

## 修复内容

- About 设置页新增“检查更新”按钮；按钮通过同源 Host route 调用与托盘相同的稳定版本检查和确认下载流程。
- 保留 GitHub Release 检查的超时、同源、稳定 SemVer 和用户确认策略。
- 恢复 `@anionex/dsh-vision-toolkit@0.1.38` 到桌面 profile 默认组件；Better Sidebar 和 Vision Toolkit 不再被迁移逻辑误删。
- 明确 README 归 `rw0104/DSH-desktop` 独立维护，禁止上游 README 覆盖产品文档。
- 清理开发阶段累计的 `.tmp-*`、历史 unpacked、备份目录和旧安装包副本；只保留源码依赖和当前发布包。

## 上游与发布规则

三仓库上游台账见 [`docs/upstream-sync.md`](../upstream-sync.md)，仓库规则见 [`AGENTS.md`](../../AGENTS.md)。

## 验证

- Update route + lifecycle：23 tests passed。
- Desktop full gate：72 files，672 passed，11 skipped；runtime closure 和 licenses 通过。
- Windows x64 NSIS installer：构建完成后补充 verifier、大小和 SHA-256。
