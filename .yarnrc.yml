enableScripts: false
nmHoistingLimits: workspaces
nodeLinker: node-modules

# This exact upstream release was audited and intentionally adopted before
# Yarn's default 24-hour registry quarantine elapsed.
npmPreapprovedPackages:
  - dsh-better-sidebar@0.16.1

# macOS releases are universal, so the physical node_modules tree must contain
# both Darwin CPU variants before Electron Builder assembles the two slices.
supportedArchitectures:
  os:
    - current
  cpu:
    - current
    - x64
    - arm64
