diff --git a/lib/client.js b/lib/client.js
index 5233c42277fc9898fd090f9ea67dbaa7ddca0a75..af70f435b8d210e460b74945b910382ade116ca2 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -397,6 +397,7 @@ window.__ModuleLoader__.load({
 		* store/verbs) + the standard locale seat.
 		* @returns the trigger and, while open, the two-level menu.
 		*/
+		function modelModalityLabel(model, t) { return [model.outputModalities?.includes("image") ? t("modelCreatesImages") : void 0, model.inputModalities?.includes("image") ? t("modelAcceptsImages") : void 0].filter(Boolean).join(" · "); }
 		function ModelSelect({ locked, available, directory, load, select, t }) {
 			const state = (0, react.useSyncExternalStore)((fn) => directory.subscribe(fn), () => directory.getSnapshot());
 			const [open, setOpen] = (0, react.useState)(false);
@@ -519,7 +520,8 @@ window.__ModuleLoader__.load({
 			};
 			const waiting = state.current === null && state.status === "loading";
 			const modelLabel = waiting ? t("trigger.loading") : currentChoice?.model.name ?? (state.current === null ? t("trigger.fallback") : `${state.current.provider}/${state.current.model}`);
-			const triggerLabel = effortLabel === void 0 ? modelLabel : `${modelLabel} · ${effortLabel}`;
+			const imageLabel = currentChoice ? modelModalityLabel(currentChoice.model, t) || void 0 : void 0;
+			const triggerLabel = [modelLabel, imageLabel, effortLabel].filter((value) => value !== void 0).join(" · ");
 			const triggerAria = waiting ? t("trigger.loading") : state.current === null ? t("trigger.selectAria") : effortLabel === void 0 ? t("trigger.aria", { model: modelLabel }) : t("trigger.ariaEffort", {
 				model: modelLabel,
 				effort: effortLabel
@@ -664,12 +666,15 @@ window.__ModuleLoader__.load({
 															model: model.id
 														});
 													},
-													children: [(0, react_jsx_runtime.jsx)("span", {
+													children: [(0, react_jsx_runtime.jsxs)("span", {
 														className: ModelSelect_module_css_default.optionCopy,
-														children: (0, react_jsx_runtime.jsx)("span", {
+														children: [(0, react_jsx_runtime.jsx)("span", {
 															className: ModelSelect_module_css_default.modelName,
 															children: model.name
-														})
+														}), (model.outputModalities?.includes("image") === true || model.inputModalities?.includes("image") === true) && (0, react_jsx_runtime.jsx)("span", {
+															className: ModelSelect_module_css_default.description,
+															children: modelModalityLabel(model, t)
+														})]
 													}), (0, react_jsx_runtime.jsx)("span", {
 														className: ModelSelect_module_css_default.check,
 														children: selected ? (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconCheckOutline16, {}) : null
@@ -752,6 +757,8 @@ window.__ModuleLoader__.load({
 			"menu.aria": "模型与推理等级",
 			"menu.model": "模型",
 			"menu.effort": "推理等级",
+			modelAcceptsImages: "图片输入",
+			modelCreatesImages: "图片输出",
 			"effort.providerDefault": "Default",
 			"status.loading": "正在刷新模型列表…",
 			"error.action": "模型操作失败：{message}",
@@ -773,6 +780,8 @@ window.__ModuleLoader__.load({
 			"menu.aria": "Model and reasoning effort",
 			"menu.model": "Model",
 			"menu.effort": "Effort",
+			modelAcceptsImages: "Image input",
+			modelCreatesImages: "Image output",
 			"effort.providerDefault": "Default",
 			"status.loading": "Refreshing model list…",
 			"error.action": "Model operation failed: {message}",
@@ -794,7 +803,7 @@ window.__ModuleLoader__.load({
 			for (const group of directory.groups) for (const model of group.models) rows.push({
 				id: rowId(group.id, model.id),
 				label: model.name,
-				detail: model.description !== void 0 ? `${group.name} · ${model.description}` : group.name,
+				detail: [group.name, modelModalityLabel(model, t) || void 0, model.description].filter((value) => value !== void 0).join(" · "),
 				...directory.current !== null && directory.current.provider === group.id && directory.current.model === model.id ? { active: true } : {}
 			});
 			for (const failure of directory.failures) rows.push({
