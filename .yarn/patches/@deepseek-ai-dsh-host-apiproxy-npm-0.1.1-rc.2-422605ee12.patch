diff --git a/lib/index.js b/lib/index.js
index 5398bcea360c1f459d02880d41b1af42eaf8e3a8..caa86d08321cc5e1267ea90896027c9823edcde7 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -508,6 +508,7 @@ const modelCatalogModelSchema = z$1.object({
 	id: z$1.string().min(1),
 	name: z$1.string().min(1),
 	description: z$1.string().optional(),
+	inputModalities: z$1.array(z$1.union([z$1.literal("text"), z$1.literal("image")])).min(1).optional(),
 	reasoning: modelReasoningSchema.optional()
 });
 /** One successfully loaded provider group. */
@@ -1025,6 +1026,7 @@ async function buildModelCatalog(ctx) {
 					id: model.id,
 					name: model.name,
 					...model.description === void 0 ? {} : { description: model.description },
+					...resolved.inputModalities === void 0 ? {} : { inputModalities: [...resolved.inputModalities] },
 					...reasoning === void 0 ? {} : { reasoning }
 				};
 			}));
diff --git a/lib/types/api/sessions.d.ts b/lib/types/api/sessions.d.ts
index 3f965d7f08b96f356976daebd64fda85dac87b66..7cb5955221076f46bbe41ea224572060239c2b55 100644
--- a/lib/types/api/sessions.d.ts
+++ b/lib/types/api/sessions.d.ts
@@ -125,6 +125,8 @@ export interface ModelCatalogModel {
     name: string;
     /** Optional provider-supplied description. */
     description?: string;
+    /** Explicit request modalities resolved for this exact model route. */
+    inputModalities?: Array<'text' | 'image'>;
     /** Exact-route reasoning metadata when the adapter exposes it. */
     reasoning?: ModelReasoning;
 }
diff --git a/lib/types/api/sessions.schema.js b/lib/types/api/sessions.schema.js
index a1304e3f92e983bd284915315e446258bd707562..c47da81074473f522ad06cdcadfd85e2603f18a2 100644
--- a/lib/types/api/sessions.schema.js
+++ b/lib/types/api/sessions.schema.js
@@ -123,6 +123,7 @@ export const modelCatalogModelSchema = z.object({
     id: z.string().min(1),
     name: z.string().min(1),
     description: z.string().optional(),
+    inputModalities: z.array(z.union([z.literal('text'), z.literal('image')])).min(1).optional(),
     reasoning: modelReasoningSchema.optional(),
 });
 /** One successfully loaded provider group. */
diff --git a/lib/types/api-proxy.js b/lib/types/api-proxy.js
index adf2670b4db79fb171e7ea3ba125c6e70d6240b2..923b104d363501379c534dafdbbc47786ab63abf 100644
--- a/lib/types/api-proxy.js
+++ b/lib/types/api-proxy.js
@@ -207,6 +207,7 @@ async function buildModelCatalog(ctx) {
                     id: model.id,
                     name: model.name,
                     ...model.description === undefined ? {} : { description: model.description },
+                    ...resolved.inputModalities === undefined ? {} : { inputModalities: [...resolved.inputModalities] },
                     ...reasoning === undefined ? {} : { reasoning },
                 };
             }));
