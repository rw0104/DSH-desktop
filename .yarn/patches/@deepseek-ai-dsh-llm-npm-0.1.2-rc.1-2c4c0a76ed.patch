diff --git a/lib/index.js b/lib/index.js
index 827dd906f5dc9b6b3d2dee436fdee8775245fa1c..c24c824aaad358551337289b603abe3d5b4151e4 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -1415,6 +1415,11 @@ let LlmRuntime = (() => {
 					id: model.id,
 					...model.name === void 0 ? {} : { name: model.name },
 					...model.contextWindow === void 0 ? {} : { contextWindow: model.contextWindow },
+					...model.inputModalities === void 0 ? {} : { inputModalities: [...model.inputModalities] },
+					...model.outputModalities === void 0 ? {} : { outputModalities: [...model.outputModalities] },
+					...model.imageGenerationApi === void 0 ? {} : { imageGenerationApi: model.imageGenerationApi },
+					...model.supportsTools === void 0 ? {} : { supportsTools: model.supportsTools },
+					...model.inputCapabilitySource === void 0 ? {} : { inputCapabilitySource: model.inputCapabilitySource },
 					...model.maxTokens === void 0 ? {} : { maxTokens: model.maxTokens }
 				});
 			}
@@ -1480,7 +1485,9 @@ let LlmRuntime = (() => {
 					id: model.id,
 					name: model.name,
 					...model.description === void 0 ? {} : { description: model.description },
-					...inputModalities === void 0 ? {} : { inputModalities }
+					...inputModalities === void 0 ? {} : { inputModalities },
+					...model.outputModalities === void 0 ? {} : { outputModalities: this.detachedModalities(model.outputModalities) },
+					...model.imageGenerationApi === void 0 ? {} : { imageGenerationApi: model.imageGenerationApi }
 				};
 			});
 		}
@@ -1515,6 +1522,8 @@ let LlmRuntime = (() => {
 				name: resolved.name,
 				...resolved.description === void 0 ? {} : { description: resolved.description },
 				...inputModalities === void 0 ? {} : { inputModalities },
+				...resolved.outputModalities === void 0 ? {} : { outputModalities: this.detachedModalities(resolved.outputModalities) },
+				...resolved.imageGenerationApi === void 0 ? {} : { imageGenerationApi: resolved.imageGenerationApi },
 				...context === void 0 ? {} : { context: { contextWindow: context.contextWindow } },
 				...defaultMaxTokens === void 0 ? {} : { defaultMaxTokens }
 			};
@@ -1606,10 +1615,13 @@ let LlmRuntime = (() => {
 			let dispatched = false;
 			return Object.freeze({
 				config: resolvedConfig,
-				retryPolicy: registration.retryPolicy,
+				// Image generation has side effects and cannot be replayed as a chat request.
+				retryPolicy: modelInfo.outputModalities?.includes("image") ? resolveRetryPolicy({ mode: "normal", maxRetries: 0 }, "llm: image generation") : registration.retryPolicy,
 				adapterDefaults,
 				...context === void 0 ? {} : { context },
 				...modelInfo.inputModalities === void 0 ? {} : { inputModalities: Object.freeze([...modelInfo.inputModalities]) },
+				...modelInfo.outputModalities === void 0 ? {} : { outputModalities: Object.freeze([...modelInfo.outputModalities]) },
+				...modelInfo.imageGenerationApi === void 0 ? {} : { imageGenerationApi: modelInfo.imageGenerationApi },
 				stream: (options) => {
 					if (dispatched) throw new LlmError("a prepared LLM call can only be dispatched once", "INVALID_PREPARED_CALL");
 					if (!callConfigEquals(options, resolvedConfig)) throw new LlmError("prepared LLM call config changed before adapter dispatch", "INVALID_PREPARED_CALL");
diff --git a/lib/typert.host.js b/lib/typert.host.js
index 1ec83654de3bc42e7ddf6c12c970c4cdbd89d010..b96e7b4c6fa7a66a541b544ddd903ec418620ff6 100644
--- a/lib/typert.host.js
+++ b/lib/typert.host.js
@@ -9,6 +9,11 @@ const _deepseek_ai_dsh_llm_llm_discoverModels_parameter_1$schema = z.object({
   'apiKey': z.string().optional(),
 })
 const _deepseek_ai_dsh_llm_llm_discoverModels_result$schema = z.array(z.object({
+  'inputModalities': z.array(z.union([z.literal("text"), z.literal("image")])).readonly().optional(),
+  'outputModalities': z.array(z.union([z.literal("text"), z.literal("image")])).readonly().optional(),
+  'imageGenerationApi': z.string().optional(),
+  'supportsTools': z.boolean().optional(),
+  'inputCapabilitySource': z.union([z.literal("provider"), z.literal("catalog"), z.literal("name"), z.literal("unknown")]).optional(),
   'id': z.string(),
   'name': z.string().optional(),
   'contextWindow': z.number().optional(),
@@ -308,7 +313,7 @@ export const TYPERT = {
           },
           {
             "name": "LlmDiscoveredModel",
-            "declaration": "export interface LlmDiscoveredModel {\n    id: string;\n    name?: string;\n    contextWindow?: number;\n    maxTokens?: number;\n}"
+            "declaration": "export interface LlmDiscoveredModel {\n    inputModalities?: readonly ModelModality[];\n    outputModalities?: readonly ModelModality[];\n    imageGenerationApi?: string;\n    supportsTools?: boolean;\n    inputCapabilitySource?: 'provider' | 'catalog' | 'name' | 'unknown';\n    id: string;\n    name?: string;\n    contextWindow?: number;\n    maxTokens?: number;\n}"
           },
           {
             "name": "LlmFailure",
@@ -332,7 +337,7 @@ export const TYPERT = {
           },
           {
             "name": "LlmModelInfo",
-            "declaration": "export interface LlmModelInfo {\n    provider: string;\n    id: string;\n    name: string;\n    description?: string;\n    inputModalities?: readonly ModelModality[];\n}"
+            "declaration": "export interface LlmModelInfo {\n    provider: string;\n    id: string;\n    name: string;\n    description?: string;\n    inputModalities?: readonly ModelModality[];\n    outputModalities?: readonly ModelModality[];\n    imageGenerationApi?: string;\n    supportsTools?: boolean;\n}"
           },
           {
             "name": "LlmModelReasoningInfo",
@@ -388,7 +393,7 @@ export const TYPERT = {
           },
           {
             "name": "PreparedLlmCall",
-            "declaration": "export interface PreparedLlmCall {\n    readonly config: LlmCallConfig;\n    readonly retryPolicy: ResolvedRetryPolicy;\n    readonly context?: LlmModelContext;\n    readonly inputModalities?: readonly ModelModality[];\n    readonly adapterDefaults: LlmCallConfigAdapterDefaults;\n    stream(options: GenerateOptions): AsyncIterable<StreamChunk>;\n}"
+            "declaration": "export interface PreparedLlmCall {\n    readonly config: LlmCallConfig;\n    readonly retryPolicy: ResolvedRetryPolicy;\n    readonly context?: LlmModelContext;\n    readonly inputModalities?: readonly ModelModality[];\n    outputModalities?: readonly ModelModality[];\n    imageGenerationApi?: string;\n    supportsTools?: boolean;\n    readonly adapterDefaults: LlmCallConfigAdapterDefaults;\n    stream(options: GenerateOptions): AsyncIterable<StreamChunk>;\n}"
           },
           {
             "name": "ProviderRequestId",
diff --git a/lib/typert.remote-client.js b/lib/typert.remote-client.js
index 0c7ead7176a87972ddf763c28d2ee961a13ad3d0..9d4a6ea6e6bcd6405b292d634e3c1dc76d7db804 100644
--- a/lib/typert.remote-client.js
+++ b/lib/typert.remote-client.js
@@ -9,6 +9,11 @@ const _deepseek_ai_dsh_llm_llm_discoverModels_parameter_1$schema = z.object({
   'apiKey': z.string().optional(),
 })
 const _deepseek_ai_dsh_llm_llm_discoverModels_result$schema = z.array(z.object({
+  'inputModalities': z.array(z.union([z.literal("text"), z.literal("image")])).readonly().optional(),
+  'outputModalities': z.array(z.union([z.literal("text"), z.literal("image")])).readonly().optional(),
+  'imageGenerationApi': z.string().optional(),
+  'supportsTools': z.boolean().optional(),
+  'inputCapabilitySource': z.union([z.literal("provider"), z.literal("catalog"), z.literal("name"), z.literal("unknown")]).optional(),
   'id': z.string(),
   'name': z.string().optional(),
   'contextWindow': z.number().optional(),
diff --git a/lib/types/types.d.ts b/lib/types/types.d.ts
index 061528bdf49f80d82e07b46ebb94d2368f26eb3e..a69bf795900e254b2f6a42d721e475f0070a4d8f 100644
--- a/lib/types/types.d.ts
+++ b/lib/types/types.d.ts
@@ -249,6 +249,12 @@ declare module '@deepseek-ai/dsh-typert-protocol' {
  * a surface adopting one of these still owes the capacities its adapter needs.
  */
 export interface LlmDiscoveredModel {
+    /** Input capability and its evidence, carried without promoting guesses to provider facts. */
+    inputModalities?: readonly ModelModality[];
+    outputModalities?: readonly ModelModality[];
+    imageGenerationApi?: string;
+    supportsTools?: boolean;
+    inputCapabilitySource?: 'provider' | 'catalog' | 'name' | 'unknown';
     /** Model id the endpoint accepts. */
     id: string;
     /** Human-readable name when the endpoint supplies one. */
@@ -270,6 +276,9 @@ export interface LlmModelInfo {
     description?: string;
     /** Accepted request modalities; absent means unknown, while an explicit omission is negative capability. */
     inputModalities?: readonly ModelModality[];
+    outputModalities?: readonly ModelModality[];
+    imageGenerationApi?: string;
+    supportsTools?: boolean;
 }
 /** Provider-owned context capacity for one exact provider/model route. */
 export interface LlmModelContext {
