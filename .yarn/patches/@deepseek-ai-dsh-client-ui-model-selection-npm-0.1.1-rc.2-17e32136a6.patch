diff --git a/lib/client.js b/lib/client.js
index d01b91620c761a741e46d9685cb9dc87562586b0..83e421c9eb0b891e2529b70121b784c0ef17dc9d 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -416,7 +416,8 @@ window.__ModuleLoader__.load({
 				select(selection).then(settleSelection);
 			};
 			const modelLabel = currentChoice?.model.name ?? t("trigger.fallback");
-			const triggerLabel = effortLabel === void 0 ? modelLabel : `${modelLabel} · ${effortLabel}`;
+			const imageLabel = currentChoice?.model.inputModalities?.includes("image") === true ? t("modelAcceptsImages") : void 0;
+			const triggerLabel = [modelLabel, imageLabel, effortLabel].filter((value) => value !== void 0).join(" · ");
 			const triggerAria = currentChoice === void 0 ? t("trigger.selectAria") : effortLabel === void 0 ? t("trigger.aria", { model: modelLabel }) : t("trigger.ariaEffort", {
 				model: modelLabel,
 				effort: effortLabel
@@ -569,6 +570,9 @@ window.__ModuleLoader__.load({
 														}), model.description !== void 0 && (0, react_jsx_runtime.jsx)("span", {
 															className: ModelSelect_module_css_default.description,
 															children: model.description
+														}), model.inputModalities?.includes("image") === true && (0, react_jsx_runtime.jsx)("span", {
+															className: ModelSelect_module_css_default.description,
+															children: t("modelAcceptsImages")
 														})]
 													}), (0, react_jsx_runtime.jsx)("span", {
 														className: ModelSelect_module_css_default.check,
@@ -654,6 +658,7 @@ window.__ModuleLoader__.load({
 			"menu.aria": "模型与推理等级",
 			"menu.model": "模型",
 			"menu.effort": "推理等级",
+			modelAcceptsImages: "支持图片",
 			"effort.providerDefault": "Default",
 			"status.loading": "正在刷新模型列表…",
 			"error.action": "模型操作失败：{message}",
@@ -674,6 +679,7 @@ window.__ModuleLoader__.load({
 			"menu.aria": "Model and reasoning effort",
 			"menu.model": "Model",
 			"menu.effort": "Effort",
+			modelAcceptsImages: "Vision",
 			"effort.providerDefault": "Default",
 			"status.loading": "Refreshing model list…",
 			"error.action": "Model operation failed: {message}",
@@ -695,7 +701,7 @@ window.__ModuleLoader__.load({
 			for (const group of directory.groups) for (const model of group.models) rows.push({
 				id: rowId(group.id, model.id),
 				label: model.name,
-				detail: model.description !== void 0 ? `${group.name} · ${model.description}` : group.name,
+				detail: [group.name, model.inputModalities?.includes("image") === true ? t("modelAcceptsImages") : void 0, model.description].filter((value) => value !== void 0).join(" · "),
 				...directory.current.provider === group.id && directory.current.model === model.id ? { active: true } : {}
 			});
 			for (const failure of directory.failures) rows.push({
