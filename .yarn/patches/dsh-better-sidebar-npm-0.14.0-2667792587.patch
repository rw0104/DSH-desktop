diff --git a/lib/client.js b/lib/client.js
index 70226f68ea5fcb478844c8366afe621665eeb907..71733f66c3eb639208f8f5a0804cc4dc63e9c104 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -1657,6 +1657,8 @@ window.__ModuleLoader__.load({
 			copy: "复制",
 			newFile: "新文件",
 			openEditor: "打开编辑器",
+			openDirectory: "在系统文件管理器中打开",
+			openDirectoryFailed: "打开目录失败",
 			gitDetail: "查看变更详情",
 			referenceFile: "@文件",
 			addToConversation: "添加到对话",
@@ -1911,6 +1913,8 @@ window.__ModuleLoader__.load({
 			copy: "Copy",
 			newFile: "New file",
 			openEditor: "Open editor",
+			openDirectory: "Open in file manager",
+			openDirectoryFailed: "Could not open directory",
 			gitDetail: "View change details",
 			referenceFile: "@file",
 			addToConversation: "Add to conversation",
@@ -2677,6 +2681,18 @@ window.__ModuleLoader__.load({
 				patch,
 				...expectedRevision !== void 0 ? { expectedRevision } : {}
 			}),
+			openDirectory: async (scope, path) => {
+				const response = await fetch("/dsh-desktop/api/open-directory", {
+					method: "POST",
+					headers: { "content-type": "application/json", "x-dsh-workbench-action": "open-directory" },
+					body: JSON.stringify({ sessionId: scope.sessionId, path })
+				});
+				const value = await response.json().catch(() => null);
+				if (!response.ok || value === null || typeof value !== "object" || value.ok !== true || typeof value.path !== "string") {
+					throw new SidebarApiError("http", value !== null && typeof value === "object" && typeof value.error === "string" ? value.error : `HTTP ${String(response.status)}`);
+				}
+				return value;
+			},
 			/** Probe a URL's response headers (the sidebar browser's embeddability
 			*  check; see the host's browser.probe route). */
 			browserProbe: (url, signal) => call("browser.probe", { url }, signal)
@@ -2811,6 +2827,7 @@ window.__ModuleLoader__.load({
 			const dataRef = (0, react.useRef)(data);
 			/** The row whose path was just copied ("copied" label replaces its button). */
 			const [copiedPath, setCopiedPath] = (0, react.useState)(null);
+			const [directoryError, setDirectoryError] = (0, react.useState)(null);
 			/** Open context menu: the row path (and whether it is a directory) plus the cursor position. */
 			const [rowMenu, setRowMenu] = (0, react.useState)(null);
 			const storeLevel = (0, react.useCallback)((path, level) => {
@@ -2892,6 +2909,13 @@ window.__ModuleLoader__.load({
 					y: event.clientY
 				});
 			};
+			const openDirectory = (path) => {
+				void api.openDirectory({ sessionId, cwd }, path).then(() => {
+					setDirectoryError(null);
+				}, (cause) => {
+					setDirectoryError(cause instanceof Error ? cause.message : String(cause));
+				});
+			};
 			/** Download a file through the host route (raw bytes, binary-safe). */
 			const downloadFile = (path) => {
 				const url = downloadUrl({
@@ -3017,12 +3041,17 @@ window.__ModuleLoader__.load({
 							children: t("referenceFile")
 						})
 					]
-				}), data[root] !== void 0 && renderLevel(root, 1)] }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Menu, {
+				}), directoryError !== null && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", { className: sidebar_module_css_default.explorerError, children: [t("openDirectoryFailed"), ": ", directoryError] }), data[root] !== void 0 && renderLevel(root, 1)] }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Menu, {
 					open: rowMenu !== null,
 					onClose: () => {
 						setRowMenu(null);
 					},
 					items: [
+						...rowMenu?.isDir === true ? [{
+							id: "open-directory",
+							label: t("openDirectory"),
+							icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconFolderOpen16, { size: 14 })
+						}] : [],
 						...rowMenu?.isDir === false && onOpenFileNewTab !== void 0 ? [{
 							id: "open-new-tab",
 							label: t("openFileNewTab"),
@@ -3065,6 +3094,10 @@ window.__ModuleLoader__.load({
 							downloadFile(target.path);
 							return;
 						}
+						if (id === "open-directory") {
+							openDirectory(target.path);
+							return;
+						}
 						copyPath(id === "relative" ? relativeTo(cwd ?? "", target.path) : target.path, target.path);
 					},
 					portal: true,
diff --git a/lib/index.js b/lib/index.js
index 83aaac8d178f437ed3ba1f5d901687bb4e5eecc9..d48062f34ca7c7c933cd5581e6dde70afa1eb464 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -2966,12 +2966,17 @@ async function attachTerminal(ctx, ptyManager, agentPtyRegistry, ws, req, resolv
 		const cwd = sessionCwdOf(ctx, sessionId, url.searchParams.get("cwd") ?? void 0);
 		const overrides = shellOverridesOf(getSettings);
 		const handle = ptyManager.open(sessionId, tabId, cwd, 80, 24, overrides.shell, overrides.shellArgs);
+		const terminalRegistry = ctx.get("workspaceTerminal");
+		terminalRegistry?.applyAdapterEvent({ sessionId, source: "ui", sourceId: tabId, kind: "attached", cwd });
 		if (handle.transcript !== "") ws.send(handle.transcript);
 		const onData = (data) => {
 			if (ws.readyState === WebSocket.OPEN && ws.bufferedAmount < 4194304) ws.send(data);
+			terminalRegistry?.applyAdapterEvent({ sessionId, source: "ui", sourceId: tabId, kind: "output", data: { text: data } });
 		};
 		const onExit = ({ exitCode }) => {
 			onData(`\r\n[process exited with code ${String(exitCode)}]\r\n`);
+			terminalRegistry?.applyAdapterEvent({ sessionId, source: "ui", sourceId: tabId, kind: "exited", data: { exitCode } });
+			if (ws.readyState === WebSocket.OPEN) ws.close(1e3, "terminal process exited");
 		};
 		const dataSub = handle.pty.onData(onData);
 		const exitSub = handle.pty.onExit(onExit);
@@ -2984,18 +2989,24 @@ async function attachTerminal(ctx, ptyManager, agentPtyRegistry, ws, req, resolv
 			} catch {}
 			if (control !== null && control.type === "close") {
 				ptyManager.scheduleClose(handle.key, 0);
+				terminalRegistry?.applyAdapterEvent({ sessionId, source: "ui", sourceId: tabId, kind: "closed", data: { reason: "user closed terminal tab" } });
 				return;
 			}
 			if (handle.exited) return;
 			if (control !== null && control.type === "resize" && typeof control.cols === "number" && typeof control.rows === "number") {
 				const dims = clampDims(control.cols, control.rows);
 				handle.pty.resize(dims.cols, dims.rows);
-			} else handle.pty.write(text);
+				terminalRegistry?.applyAdapterEvent({ sessionId, source: "ui", sourceId: tabId, kind: "resized", data: dims });
+			} else {
+				handle.pty.write(text);
+				terminalRegistry?.applyAdapterEvent({ sessionId, source: "ui", sourceId: tabId, kind: "input", data: { text } });
+			}
 		});
 		ws.on("close", () => {
 			dataSub.dispose();
 			exitSub.dispose();
 			ptyManager.scheduleClose(handle.key, resolved.reconnectGraceMs);
+			terminalRegistry?.applyAdapterEvent({ sessionId, source: "ui", sourceId: tabId, kind: "disconnected", data: { reason: "socket closed" } });
 		});
 	} catch (error) {
 		ws.close(1011, error instanceof Error ? error.message : String(error));
diff --git a/src/client/FileTree.tsx b/src/client/FileTree.tsx
index 7e85a9470eca466058f860861db503986c2ac8b5..11bbc2072a83f1313fb636b3d174a27d39f27123 100644
--- a/src/client/FileTree.tsx
+++ b/src/client/FileTree.tsx
@@ -61,6 +61,7 @@ export function FileTree(props: {
   const dataRef = useRef(data)
   /** The row whose path was just copied ("copied" label replaces its button). */
   const [copiedPath, setCopiedPath] = useState<string | null>(null)
+  const [directoryError, setDirectoryError] = useState<string | null>(null)
   /** Open context menu: the row path (and whether it is a directory) plus the cursor position. */
   const [rowMenu, setRowMenu] = useState<{ path: string; isDir: boolean; x: number; y: number } | null>(null)
 
@@ -136,6 +137,14 @@ export function FileTree(props: {
     setRowMenu({ path, isDir, x: event.clientX, y: event.clientY })
   }
 
+  const openDirectory = (path: string): void => {
+    void api.openDirectory({ sessionId, cwd }, path).then(() => {
+      setDirectoryError(null)
+    }, (cause: unknown) => {
+      setDirectoryError(cause instanceof Error ? cause.message : String(cause))
+    })
+  }
+
   /** Download a file through the host route (raw bytes, binary-safe). */
   const downloadFile = (path: string): void => {
     const url = downloadUrl({ sessionId, cwd }, path)
@@ -246,6 +255,7 @@ export function FileTree(props: {
                 </button>
               )}
           </div>
+          {directoryError !== null && <div className={css.explorerError}>{t('openDirectoryFailed')}: {directoryError}</div>}
           {data[root] !== undefined && renderLevel(root, 1)}
         </>
       )}
@@ -257,6 +267,9 @@ export function FileTree(props: {
         open={rowMenu !== null}
         onClose={() => { setRowMenu(null) }}
         items={[
+          ...(rowMenu?.isDir === true
+            ? [{ id: 'open-directory', label: t('openDirectory'), icon: <IconFolderOpen16 size={14} /> }]
+            : []),
           // The open escapes head the FILE menu (dirs only get copy).
           ...(rowMenu?.isDir === false && onOpenFileNewTab !== undefined
             ? [{ id: 'open-new-tab', label: t('openFileNewTab'), icon: <IconCodeOutline16 size={14} /> }]
@@ -287,6 +300,10 @@ export function FileTree(props: {
             downloadFile(target.path)
             return
           }
+          if (id === 'open-directory') {
+            openDirectory(target.path)
+            return
+          }
           copyPath(
             id === 'relative' ? relativeTo(cwd ?? '', target.path) : target.path,
             target.path,
diff --git a/src/client/api.ts b/src/client/api.ts
index 216916a5fbc54aac2685f1228583a703f0981b78..1a9d12f2d03ec8282a7dac653be0e4b38773b447 100644
--- a/src/client/api.ts
+++ b/src/client/api.ts
@@ -249,3 +249,20 @@ function fileUrl(scope: SessionScope, path: string, download: boolean): string {
 export function htmlUrl(scope: SessionScope, path: string): string {
   return encodeHtmlUrl(scope.sessionId, path)
 }
+  /** Ask DSH Desktop to open one Session-validated directory. */
+  openDirectory: async (scope: SessionScope, path: string): Promise<{ ok: true; path: string }> => {
+    const response = await fetch('/dsh-desktop/api/open-directory', {
+      method: 'POST',
+      headers: { 'content-type': 'application/json', 'x-dsh-workbench-action': 'open-directory' },
+      body: JSON.stringify({ sessionId: scope.sessionId, path }),
+    })
+    const value: unknown = await response.json().catch(() => null)
+    if (!response.ok || value === null || typeof value !== 'object'
+      || (value as { ok?: unknown }).ok !== true || typeof (value as { path?: unknown }).path !== 'string') {
+      const error = value !== null && typeof value === 'object' && typeof (value as { error?: unknown }).error === 'string'
+        ? (value as { error: string }).error
+        : `HTTP ${String(response.status)}`
+      throw new SidebarApiError('http', error)
+    }
+    return value as { ok: true; path: string }
+  },
diff --git a/src/client/locales.ts b/src/client/locales.ts
index d89900d912c8056ea419efe668d583d7c0e9a39a..e6d68593c5a5af73e172101ae8ed08cd388002ec 100644
--- a/src/client/locales.ts
+++ b/src/client/locales.ts
@@ -123,6 +123,8 @@ export const zh = {
   copy: '复制',
   newFile: '新文件',
   openEditor: '打开编辑器',
+  openDirectory: '在系统文件管理器中打开',
+  openDirectoryFailed: '打开目录失败',
   gitDetail: '查看变更详情',
   referenceFile: '@文件',
   addToConversation: '添加到对话',
@@ -378,6 +380,8 @@ export const en: Record<keyof typeof zh, string> = {
   copy: 'Copy',
   newFile: 'New file',
   openEditor: 'Open editor',
+  openDirectory: 'Open in file manager',
+  openDirectoryFailed: 'Could not open directory',
   gitDetail: 'View change details',
   referenceFile: '@file',
   addToConversation: 'Add to conversation',
diff --git a/src/index.ts b/src/index.ts
index baeb387e4e68d4e879ebf49f2c105d8c4b112f30..9004fecde7236399f2dfc79eb739bc88a1459d45 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -882,15 +882,29 @@ async function attachTerminal(
     // terminals opened from now on (existing pty handles keep their shell).
     const overrides = shellOverridesOf(getSettings)
     const handle = ptyManager.open(sessionId, tabId, cwd, 80, 24, overrides.shell, overrides.shellArgs)
+    const terminalRegistry = ctx.get('workspaceTerminal') as {
+      applyAdapterEvent(event: {
+        sessionId: string
+        source: 'ui'
+        sourceId: string
+        kind: 'attached' | 'disconnected' | 'input' | 'output' | 'resized' | 'exited' | 'closed'
+        cwd?: string
+        data?: Record<string, unknown>
+      }): void
+    } | undefined
+    terminalRegistry?.applyAdapterEvent({ sessionId, source: 'ui', sourceId: tabId, kind: 'attached', cwd })
     // Replay the transcript, then follow live output.
     if (handle.transcript !== '') ws.send(handle.transcript)
     const onData = (data: string): void => {
       if (ws.readyState === WebSocket.OPEN && ws.bufferedAmount < 4 * 1024 * 1024) {
         ws.send(data)
       }
+      terminalRegistry?.applyAdapterEvent({ sessionId, source: 'ui', sourceId: tabId, kind: 'output', data: { text: data } })
     }
     const onExit = ({ exitCode }: { exitCode: number; signal?: number }): void => {
       onData(`\r\n[process exited with code ${String(exitCode)}]\r\n`)
+      terminalRegistry?.applyAdapterEvent({ sessionId, source: 'ui', sourceId: tabId, kind: 'exited', data: { exitCode } })
+      if (ws.readyState === WebSocket.OPEN) ws.close(1000, 'terminal process exited')
     }
     const dataSub = handle.pty.onData(onData)
     const exitSub = handle.pty.onExit(onExit)
@@ -910,6 +924,7 @@ async function attachTerminal(
       if (control !== null && control.type === 'close') {
         // The owning tab was closed: release the quota immediately.
         ptyManager.scheduleClose(handle.key, 0)
+        terminalRegistry?.applyAdapterEvent({ sessionId, source: 'ui', sourceId: tabId, kind: 'closed', data: { reason: 'user closed terminal tab' } })
         return
       }
       if (handle.exited) return
@@ -920,8 +935,10 @@ async function attachTerminal(
       ) {
         const dims = clampDims(control.cols, control.rows)
         handle.pty.resize(dims.cols, dims.rows)
+        terminalRegistry?.applyAdapterEvent({ sessionId, source: 'ui', sourceId: tabId, kind: 'resized', data: dims })
       } else {
         handle.pty.write(text)
+        terminalRegistry?.applyAdapterEvent({ sessionId, source: 'ui', sourceId: tabId, kind: 'input', data: { text } })
       }
     })
     ws.on('close', () => {
@@ -931,6 +948,7 @@ async function attachTerminal(
       // for a grace period so a quick reconnect keeps it; the reconnect's
       // open() cancels the pending close.
       ptyManager.scheduleClose(handle.key, resolved.reconnectGraceMs)
+      terminalRegistry?.applyAdapterEvent({ sessionId, source: 'ui', sourceId: tabId, kind: 'disconnected', data: { reason: 'socket closed' } })
     })
   } catch (error) {
     ws.close(1011, error instanceof Error ? error.message : String(error))
